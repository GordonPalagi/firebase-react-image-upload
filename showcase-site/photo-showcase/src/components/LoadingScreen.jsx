import React from 'react';

const LoadingScreen = () => {
    return (
        <div className="loading-screen">
            I'm a loading screen!
        </div>
    );
};

export default LoadingScreen;
